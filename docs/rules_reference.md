# docs 규칙 문서 (참조)

## 설계 원칙
- 분석 단위는 증거(`evidence`)와 해석(`interpretation`)의 분리 구조를 따른다.
- 개인정보 필드는 원문 보관 대신 마스킹값 저장을 기본으로 한다.
- 섹션별 추적성을 위해 `source_lines`(원문 행번호)를 유지한다.
- 분석 축은 기본 3축(학업/진로/공동체), 필요 시 4축 확장(`axis4`) 허용.
- 수상/자격증/진로희망은 메타 정보로만 저장하고 핵심 평가 근거에서 제외한다.
